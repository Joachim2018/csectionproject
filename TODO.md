# TODO

- [x] Update `styles.css` so the logo inside `.image-card` scales responsively and remains fully visible.
- [ ] Reload `index.html` and verify the logo in the About section displays correctly.
